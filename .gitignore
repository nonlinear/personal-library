# Books metadata/indices (books themselves excluded via .git/info/exclude + .vscode/settings.json)
books/**/*.pkl
books/**/*.index
books/metadata.json

# Failed books log (contains PII)
engine/docs/FAILED.md

# VS Code settings
.vscode/

# GitHub workflows/scripts (except research prompt)
.github/
!.github/prompts/research.prompt.md
!.github/prompts/whatsup.prompt.md

# Storage (deprecated - now inside books/)
storage/

# Embedding models (large downloads, rebuild locally)
models/

# Environment
.env

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Legacy files (migration artifacts)
*.rag-topics
indexed_books.json
indexed_books.txt
query_result.txt

# Books (binary files)
books/**/*.pdf
books/**/*.epub
books/**/*.txt
